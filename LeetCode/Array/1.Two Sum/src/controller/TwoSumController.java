package controller;

import java.awt.event.ActionEvent;

/**
 * The TwoSumController interface defines the contract for the controller component
 * in the Model-View-Controller (MVC) architecture for the "Two Sum" problem.
 * The controller is responsible for coordinating between the Model and the View.
 */
public interface TwoSumController {
  /**
   * The run method serves as the entry point for the controller's logic.
   * It is responsible for orchestrating the flow of data between the Model and the View,
   * including user input and output.
   */
  void run();

}
