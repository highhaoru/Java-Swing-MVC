package controller;

/**
 * The Controller interface specifies methods that all controllers should implement.
 *
 * @author Hao Jin
 */
public interface PersonController {
  /**
   * Initialize the controller.
   */
  void init();
}
